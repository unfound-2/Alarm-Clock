# WakeGuard

WakeGuard is a Flutter companion app for an autonomous BLE smart alarm clock. It configures alarms and timers, synchronizes clock time/settings over an HM-10 BLE link, and uses printed QR codes to make alarm dismissal require physical movement.
